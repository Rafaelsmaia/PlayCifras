node_modules
.next

# Keep environment variables out of version control
.env

# Banco local SQLite (grande; não versionar — use Postgres/migração em produção)
prisma/dev.db

/app/generated/prisma

# Dataset Kaggle (grande; não versionar)
archive.zip
data/kaggle-extract/

.vercel
